server:
  port: @server.port@

spring:
  application:
    name: @sys.app.name@-@sys.app.project@-@profile.active@
  profiles:
    active: @profile.active@
    include:
      - core
      - log
      - doc
      - web
      - sys
      - mysql
      - redis